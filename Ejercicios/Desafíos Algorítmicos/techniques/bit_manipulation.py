# techniques/bit_manipulation.py


# Ejercicio 1: Contar el número de bits 1 en un número (Popcount)
def contar_bits_1(n: int) -> int:
    """
    Cuenta el número de bits 1 en la representación binaria de un número.

    Parámetros:
    - n: El número entero en el que se cuentan los bits 1.

    Retorna:
    - El número de bits 1 en la representación binaria de n.
    """
    pass


# Ejercicio 2: Comprobar si un número es potencia de 2
def es_potencia_de_2(n: int) -> bool:
    """
    Verifica si un número es una potencia de 2 utilizando manipulación de bits.

    Parámetros:
    - n: El número entero a verificar.

    Retorna:
    - True si n es una potencia de 2, False en caso contrario.
    """
    pass


# Ejercicio 3: Invertir los bits de un número
def invertir_bits(n: int, bits: int) -> int:
    """
    Invierte los bits de un número. El número de bits se especifica.

    Parámetros:
    - n: El número entero a invertir.
    - bits: El número total de bits en la representación binaria del número.

    Retorna:
    - El número resultante tras invertir los bits de n.
    """
    pass


# Ejercicio 4: Obtener el bit en una posición específica
def obtener_bit(n: int, pos: int) -> int:
    """
    Obtiene el bit en una posición específica de un número.

    Parámetros:
    - n: El número entero en el que se obtiene el bit.
    - pos: La posición del bit que se desea obtener (0-indexed).

    Retorna:
    - El valor del bit en la posición especificada (0 o 1).
    """
    pass


# Ejercicio 5: Establecer un bit en una posición específica
def establecer_bit(n: int, pos: int) -> int:
    """
    Establece un bit en una posición específica de un número (lo pone en 1).

    Parámetros:
    - n: El número entero en el que se establece el bit.
    - pos: La posición del bit que se desea establecer (0-indexed).

    Retorna:
    - El número resultante tras establecer el bit en la posición especificada.
    """
    pass


# Ejercicio 6: Borrar un bit en una posición específica
def borrar_bit(n: int, pos: int) -> int:
    """
    Borra (pone a 0) el bit en una posición específica de un número.

    Parámetros:
    - n: El número entero en el que se borra el bit.
    - pos: La posición del bit que se desea borrar (0-indexed).

    Retorna:
    - El número resultante tras borrar el bit en la posición especificada.
    """
    pass


# Ejercicio 7: Comprobar si dos números tienen el mismo signo
def mismos_signos(a: int, b: int) -> bool:
    """
    Verifica si dos números enteros tienen el mismo signo utilizando manipulación de bits.

    Parámetros:
    - a: El primer número entero.
    - b: El segundo número entero.

    Retorna:
    - True si ambos números tienen el mismo signo, False en caso contrario.
    """
    pass


# Ejercicio 8: Intercambiar dos números sin usar una variable temporal
def intercambiar_sin_temporal(a: int, b: int) -> tuple[int, int]:
    """
    Intercambia dos números utilizando operaciones de manipulación de bits sin usar una variable temporal.

    Parámetros:
    - a: El primer número entero.
    - b: El segundo número entero.

    Retorna:
    - Una tupla con los números intercambiados.
    """
    pass


# Ejercicio 9: Comprobar si un número es impar o par usando bits
def es_impar(n: int) -> bool:
    """
    Verifica si un número es impar utilizando manipulación de bits.

    Parámetros:
    - n: El número entero a verificar.

    Retorna:
    - True si el número es impar, False si es par.
    """
    pass


# Ejercicio 10: Contar cuántos números tienen exactamente k bits 1 en su representación binaria
def contar_con_k_bits_1(arr: list[int], k: int) -> int:
    """
    Cuenta cuántos números en un arreglo tienen exactamente k bits 1 en su representación binaria.

    Parámetros:
    - arr: Una lista de números enteros.
    - k: El número de bits 1 que deben tener los números en su representación binaria.

    Retorna:
    - El número de números en el arreglo que tienen exactamente k bits 1.
    """
    pass


# Ejercicio 11: Cambiar el valor de un bit en una posición específica
def cambiar_bit(n: int, pos: int, valor: int) -> int:
    """
    Cambia el valor de un bit en una posición específica de un número.

    Parámetros:
    - n: El número entero en el que se cambia el bit.
    - pos: La posición del bit que se desea cambiar (0-indexed).
    - valor: El nuevo valor del bit (0 o 1).

    Retorna:
    - El número resultante tras cambiar el valor del bit.
    """
    pass


# Ejercicio 12: Verificar si dos números tienen el mismo número de bits 1
def mismos_bits_1(a: int, b: int) -> bool:
    """
    Verifica si dos números tienen el mismo número de bits 1 en su representación binaria.

    Parámetros:
    - a: El primer número entero.
    - b: El segundo número entero.

    Retorna:
    - True si ambos números tienen el mismo número de bits 1, False en caso contrario.
    """
    pass
