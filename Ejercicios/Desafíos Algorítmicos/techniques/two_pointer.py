# techniques/two_pointer.py


# Ejercicio 1: Encontrar un par que sume a un valor objetivo
def encontrar_par_suma_objetivo(
    arr: list[int], objetivo: int
) -> tuple[int, int] | None:
    """
    Encuentra dos elementos en un arreglo cuya suma sea igual al valor objetivo utilizando la técnica de dos punteros.

    Parámetros:
    - arr: Un arreglo de números enteros.
    - objetivo: El valor objetivo que queremos encontrar sumando dos elementos.

    Retorna:
    - Un par de elementos cuyo valor sumado es igual al objetivo, o None si no existe tal par.
    """
    pass


# Ejercicio 2: Verificar si existe un subarreglo con la suma igual a un valor objetivo
def subarreglo_suma_objetivo(arr: list[int], objetivo: int) -> bool:
    """
    Verifica si existe un subarreglo cuya suma sea igual al valor objetivo utilizando la técnica de dos punteros.

    Parámetros:
    - arr: Un arreglo de números enteros.
    - objetivo: El valor objetivo de la suma del subarreglo.

    Retorna:
    - True si existe un subarreglo cuya suma es igual al valor objetivo, False si no.
    """
    pass


# Ejercicio 3: Encontrar la subcadena más corta que contenga todos los caracteres de un conjunto
def subcadena_minima_conjunto(s: str, conjunto: set[str]) -> str:
    """
    Encuentra la subcadena más corta de una cadena que contiene todos los caracteres de un conjunto dado utilizando la técnica de dos punteros.

    Parámetros:
    - s: Una cadena de caracteres.
    - conjunto: Un conjunto de caracteres que deben aparecer en la subcadena.

    Retorna:
    - La subcadena más corta que contiene todos los caracteres del conjunto, o una cadena vacía si no existe tal subcadena.
    """
    pass


# Ejercicio 4: Mover ceros al final de un arreglo sin cambiar el orden relativo de los elementos
def mover_ceros(arr: list[int]) -> list[int]:
    """
    Mueve todos los ceros en un arreglo al final mientras mantiene el orden relativo de los demás elementos utilizando la técnica de dos punteros.

    Parámetros:
    - arr: Un arreglo de números enteros que puede contener ceros.

    Retorna:
    - El arreglo con todos los ceros movidos al final.
    """
    pass


# Ejercicio 5: Verificar si un arreglo tiene un ciclo
def tiene_ciclo(arr: list[int]) -> bool:
    """
    Verifica si un arreglo tiene un ciclo (es decir, si puede formar un ciclo mediante alguna combinación de elementos) utilizando dos punteros.

    Parámetros:
    - arr: Un arreglo de números enteros.

    Retorna:
    - True si existe un ciclo en el arreglo, False si no.
    """
    pass


# Ejercicio 6: Encontrar la longitud de la subcadena más larga con caracteres únicos
def longitud_subcadena_unica(s: str) -> int:
    """
    Encuentra la longitud de la subcadena más larga que no contiene caracteres repetidos utilizando la técnica de dos punteros.

    Parámetros:
    - s: Una cadena de caracteres.

    Retorna:
    - La longitud de la subcadena más larga con caracteres únicos.
    """
    pass


# Ejercicio 7: Partición de un arreglo en base a un pivote
def particion_arr_pivote(arr: list[int], pivote: int) -> list[int]:
    """
    Particiona un arreglo de manera que todos los elementos menores que el pivote estén a la izquierda y los mayores a la derecha utilizando dos punteros.

    Parámetros:
    - arr: Un arreglo de números enteros.
    - pivote: El valor del pivote utilizado para la partición.

    Retorna:
    - El arreglo particionado.
    """
    pass


# Ejercicio 8: Verificar si un arreglo está ordenado
def esta_ordenado(arr: list[int]) -> bool:
    """
    Verifica si un arreglo está ordenado de manera ascendente utilizando la técnica de dos punteros.

    Parámetros:
    - arr: Un arreglo de números enteros.

    Retorna:
    - True si el arreglo está ordenado ascendentemente, False si no.
    """
    pass


# Ejercicio 9: Encontrar la diferencia mínima entre dos elementos en un arreglo
def diferencia_minima(arr: list[int]) -> int:
    """
    Encuentra la diferencia mínima entre dos elementos en un arreglo utilizando la técnica de dos punteros.

    Parámetros:
    - arr: Un arreglo de números enteros.

    Retorna:
    - La diferencia mínima entre dos elementos del arreglo.
    """
    pass


# Ejercicio 10: Determinar si un arreglo tiene dos elementos cuyo producto sea igual a un valor objetivo
def producto_objetivo(arr: list[int], objetivo: int) -> bool:
    """
    Verifica si existe un par de elementos en un arreglo cuyo producto sea igual al valor objetivo utilizando la técnica de dos punteros.

    Parámetros:
    - arr: Un arreglo de números enteros.
    - objetivo: El valor objetivo que queremos encontrar como producto de dos elementos.

    Retorna:
    - True si existe un par de elementos cuyo producto es igual al objetivo, False si no.
    """
    pass
